import styled from "styled-components";

export const Title = styled.div`
    color: var(--blue);
    font-size: 1.2rem;
    font-weight: 500;
    width: 100%;
`;

export const Content = styled.div`
    color: var(--blue);
    font-size: 1rem;
    width: 100%;
`;
